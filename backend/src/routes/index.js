const { usersRouter, schoolsRouter, examsRouter, reportsRouter, curriculumRouter } = require('./combined');
module.exports = { usersRouter, schoolsRouter, examsRouter, reportsRouter, curriculumRouter };
